name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  example_matrix:
    strategy:
      matrix:
        node_version: [16, 18, 20]
    uses: ./.github/workflows/lint_format_build.yml
    with:
      node_version: ${{ matrix.node_version }}
